{
  "name": "cbcheckout",
  "version": "1.0.0",
  "private": true,
  "description": "Live Chromebook checkout app with barcode scanning.",
  "scripts": {
    "start": "node server.js",
    "check": "node --check app.js && node --check server.js"
  },
  "engines": {
    "node": ">=18"
  }
}
